# CLI Gateway

统一消息网关 + 多 CLI Agent 代理系统

通过 Telegram Bot 与后端 CLI 工具（Claude Code、Codex、Gemini）交互，实现"用聊天代替终端"的体验。

## 功能特性

- 🤖 **多 CLI Agent 支持**：Claude Code、Codex、Gemini CLI
- 💬 **多通道支持**：Telegram（已实现）、Discord/Email（规划中）
- 📝 **会话管理**：每个用户可创建多个独立会话
- 📁 **文件交互**：通过聊天上传/下载文件到会话工作目录
- 🔒 **白名单鉴权**：仅授权用户可访问
- 📊 **进程监控**：Watchdog 自动监控和恢复异常进程
- 🔄 **会话持久化**：服务重启后自动恢复会话

## 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 配置

```bash
# 复制配置模板
cp config.example.yaml config.yaml

# 编辑配置文件
nano config.yaml
```

设置环境变量：

```bash
export TG_BOT_TOKEN="your-telegram-bot-token"
```

### 3. 运行

```bash
python main.py
```

## 用户命令

| 命令 | 说明 |
|------|------|
| `/start` | 显示欢迎信息和可用 Agent |
| `/help` | 显示命令帮助 |
| `/agent <name>` | 创建新会话并切换到指定 Agent |
| `/sessions` | 列出所有活跃会话 |
| `/current` | 显示当前会话信息 |
| `/switch <id>` | 切换到已有会话 |
| `/kill [id]` | 终止会话 |
| `/cancel` | 中断当前操作 |
| `/file list` | 列出工作目录文件 |
| `/file get <name>` | 下载文件 |

## 架构

```
┌─────────────┐   ┌──────────────┐   ┌─────────────┐
│  Telegram   │──▶│   Router     │──▶│   Agents    │
│  Channel    │◀──│   + Auth     │◀──│  (Claude,   │
└─────────────┘   └──────────────┘   │   Codex)    │
                                      └─────────────┘
                         │
                         ▼
                  ┌──────────────┐
                  │   Session    │
                  │   Manager    │
                  └──────────────┘
```

## 开发路线

- [x] **Phase 1**：MVP（基本消息收发 + Claude Code）
- [ ] **Phase 2**：体验优化（流式输出、格式化、文件交互）
- [ ] **Phase 3**：稳定性（Watchdog、多 Agent、群聊支持）
- [ ] **Phase 4**：扩展（Discord、Email 通道）

## systemd 部署

```bash
# 复制服务文件
sudo cp systemd/cli-gateway.service /etc/systemd/system/

# 编辑服务文件中的路径和环境变量
sudo nano /etc/systemd/system/cli-gateway.service

# 启动服务
sudo systemctl daemon-reload
sudo systemctl enable cli-gateway
sudo systemctl start cli-gateway

# 查看状态
sudo systemctl status cli-gateway
```

## 许可证

MIT License
